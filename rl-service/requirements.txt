# RL Trading Service Dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
numpy>=1.24.0
pandas>=2.0.0

# Real RL Training (CPU-only to keep image small)
stable-baselines3>=2.2.1
gymnasium>=0.29.1
tqdm>=4.65.0
rich>=13.0.0

# PyTorch CPU-only (much smaller than CUDA version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
